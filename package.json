{
  "name": "hardhat-project",
  "scripts": {
    "prepare": "husky install",
    "clean": "rm -rf abi artifacts cache coverage docs && rm -f coverage.json",
    "build": "npx hardhat compile",
    "test": "npx hardhat test",
    "generate-doc": "run hardhat docgen",
    "coverage": "node --max-old-space-size=4096 ./node_modules/.bin/hardhat coverage --network hardhat --solcoverjs ./.solcover.js",
    "slither": "pip3 install --user slither-analyzer && slither .",
    "sizer": "npx hardhat size-contracts",
    "check": "npx hardhat check",
    "lint": "node ./node_modules/.bin/solhint -f table contracts/**/*.sol",
    "lint:fix": "node ./node_modules/.bin/solhint contracts/**/*.sol --fix",
    "secret-scan": "pip3 install --user trufflehog3 && trufflehog3 --no-history -v"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.2",
    "@nomiclabs/hardhat-etherscan": "^2.1.4",
    "@nomiclabs/hardhat-ganache": "^2.0.1",
    "@nomiclabs/hardhat-solhint": "^2.0.0",
    "@nomiclabs/hardhat-waffle": "^2.0.1",
    "chai": "^4.3.4",
    "ethereum-waffle": "^3.4.0",
    "ethers": "^5.4.4",
    "hardhat": "^2.6.0",
    "hardhat-deploy": "^0.8.11",
    "hardhat-gas-reporter": "^1.0.4",
    "husky": "^7.0.1",
    "prettier": "^2.3.2",
    "prettier-plugin-solidity": "^1.0.0-beta.17",
    "solhint": "^3.3.6",
    "solhint-plugin-prettier": "^0.0.5",
    "solidity-coverage": "^0.7.16"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "hardhat-abi-exporter": "^2.2.1",
    "hardhat-contract-sizer": "^2.0.3",
    "hardhat-docgen": "^1.1.1",
    "hardhat-preprocessor": "^0.1.4",
    "hardhat-watcher": "^2.1.1"
  }
}
